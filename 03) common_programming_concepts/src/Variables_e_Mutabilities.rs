fn main() {
    /*
    VAR IMMUTABILI DI DEFAULT:
        let x = 5;

        x = 6;  ERROR..

    - - - -
    VAR MUTABLE:
        let mut x = 5;

        x = 6;   OK..

    - - - -
    VAR COSTANTI, va messo il tipo:   (SI -> var. globali, SI -> assegnate SOLO a espressioni costanti, SI -> UPPERCASE)

        const THREE_HOURS_IN_SECONDS: u32 = 60 * 60 * 3;

    - - - -
    SHADOWING: (serve per modificare una variabile immutabile OPPURE per cambiare il tipo della variabile)
        let x = 5;
        let x = x + 1;    // x = 6

        {
            let x = x * 2;
            println!("Inner scope x value -> {x}");     // x = 12
        }

        println!("x value -> {x}");    // x = 6

    - - - -
    */
}
